<?php
/**
 * Copy this file to config.local.php and fill in your hosting MySQL credentials.
 * config.local.php is ignored by git — never commit real passwords.
 */

define('DB_HOST', 'localhost');
define('DB_NAME', 'your_database_name');
define('DB_USER', 'your_mysql_user');
define('DB_PASS', 'your_mysql_password');

// Allow your site origin (or keep * for testing)
define('CORS_ALLOWED_ORIGINS', '*');

// Set true temporarily to see detailed SQL errors in API responses
define('APP_DEBUG', false);
